import { Command, flags } from '@heroku-cli/command';
export default class RunRake extends Command {
    static hidden: boolean;
    static strict: boolean;
    static flags: {
        app: flags.IOptionFlag<string>;
        remote: flags.IOptionFlag<string>;
        size: flags.IOptionFlag<string>;
        'exit-code': import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
        env: flags.IOptionFlag<string>;
        'no-tty': import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
    };
    run(): Promise<void>;
}
