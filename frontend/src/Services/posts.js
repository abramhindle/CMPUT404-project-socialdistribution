import axios from "axios";
import api from "../API/api";
// import {getCookie} from "../pages/login/utilities";

// axios.post("/api/authors/logout/", {}, {headers: {"Authorization": "Token " + localStorage.getItem("token")}})
// export function getPrize(id){
//     const csrfToken = getCookie('csrftoken');
//     return axios.get("/rewards/prizes/" + id + '/', {headers: {'X-CSRFToken': csrfToken}})
// }

